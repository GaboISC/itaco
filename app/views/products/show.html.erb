<h2>Products2</h2>
<div id="products-catalog" class="panel panel-default">
<div class="panel-heading"> Catálogo De Productos2 </div>
	<table class="table">
		<th>ID</th>
		<th>Nombre</th>
		<th>Precio</th>
		<th>Action</th>
		<% @products.each do |p| %>
		<tr>
		<td><%=	p.id	%>	</td>
		<td><%=	p.name	%>	</td>
		<td><%= p.price %>	</td>

		<td><%= form_for @x do |op| %>
			<%= op.button  :submit , name: "quantity",value: "1", name: "product_id",value: p.id, %> <%end%>  </td>
		</tr>
		<% end %>
	</table>
</div>